import { div, form, button, textarea, a, span, p, strong, h2, ul, li, em, small } from '@cycle/dom'

const websocket = websoc => 
  div([
      console.log(websoc.rune)
  ])
module.exports = {websocket}